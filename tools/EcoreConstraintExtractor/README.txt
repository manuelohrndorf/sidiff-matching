This XSLT-Transformation can extract eAnnotation with Constraints from an
ecore meta-model into a txt file.

How to use it:
- make sure your have a XSLT processor installed (e.g. Eclipse XSL Developer Tools)
- Right-click the xsl-File > run-as > XSL-Transformation and select the ecore file.